<%= form_for @drug, :html => { :multipart => true } do |f| %>
  <% if @drug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@drug.errors.count, "error") %> prohibited this drug from being saved:</h2>

      <ul>
      <% @drug.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
 <%= f.label :disease_id %><br>
 <%= f.select :disease_id, 
options_for_select(Disease.all.collect {|a| ["#{a.name}", a.id]}, selected: (@drug.disease ? @drug.disease.id : Disease.first.id)), {} %>
</div>
  <div class="field">
    <%= f.label :uses %><br>
    <%= f.text_area :uses %>
  </div>
  <div class="field">
    <%= f.label :sideeffects %><br>
    <%= f.text_area :sideeffects %>
  </div>
  <div class="field">
    <%= f.label :precautions %><br>
    <%= f.text_area :precautions %>
  </div>
  <div class="field">
    <%= f.label :interactions %><br>
    <%= f.text_area :interactions %>
  </div>
  <div class="field">
    <%= f.label :overdose %><br>
    <%= f.text_area :overdose %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.file_field :image %>
  </div>
   <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
